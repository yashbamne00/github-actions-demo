name : CICD
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on : ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: libaray
        run: pip install -r requirements.txt
      - name: test
        run: python -m pytest
      - name: deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{secrets.EC2_HOST}}
          username: ${{secrets.EC2_USER}}
          key: ${{secrets.EC2_KEY}}
          script: |
            mkdir ~/webapp/github-actions-cicd-pipeline
            cd ~/webapp/github-actions-cicd-pipeline
            git pull
            docker-compose up -d --build
         
        
